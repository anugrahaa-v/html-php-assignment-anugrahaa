<?php
$password = "Passw0rd!";

$pattern = '/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[\W_]).{8,}$/';

if (preg_match($pattern, $password)) {
    echo "Valid password.";
} else {
    echo "Invalid password.";
}
?>
