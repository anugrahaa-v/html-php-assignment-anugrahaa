<?php
$number = (int)readline("Enter a number: ");
$sum = 0;
$temp = $number;

while ($temp > 0) {
    $digit = $temp % 10;
    $sum += $digit;
    $temp = (int)($temp / 10);
}

echo "Sum of the digits of $number is: $sum\n";
?>
